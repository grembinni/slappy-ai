---
phase: 02-two-characters-move
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - settings.py
autonomous: true
requirements:
  - MOV-01
  - MOV-02
  - MOV-03
  - MOV-08

must_haves:
  truths:
    - "settings.PLAYER_SPEED_UP == 200"
    - "settings.PLAYER_SPEED_H == 300"
    - "settings.PLAYER_SPEED_DOWN == 400"
    - "settings.ANIM_INTERVAL == 0.15"
  artifacts:
    - path: "settings.py"
      provides: "Phase 2 movement speed and animation constants"
      contains: "PLAYER_SPEED_UP = 200"
  key_links:
    - from: "settings.py"
      to: "player.py"
      via: "from settings import PLAYER_SPEED_UP, PLAYER_SPEED_H, PLAYER_SPEED_DOWN, ANIM_INTERVAL"
      pattern: "from settings import"
---

<objective>
Append four Phase 2 movement constants to settings.py under a new comment block. These values are the single source of truth for all player speed and animation timing — player.py imports them directly and never defines its own numeric literals.

Purpose: Phase 2 speed constants must live in settings.py before player.py is written. Wrong values here cascade into every movement test.
Output: settings.py extended with PLAYER_SPEED_UP, PLAYER_SPEED_H, PLAYER_SPEED_DOWN, ANIM_INTERVAL
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-two-characters-move/02-CONTEXT.md
@settings.py
</context>

<interfaces>
<!-- Existing settings.py constants (do NOT change these): -->
<!-- SCREEN_W = 1280, SCREEN_H = 800, FPS = 60, SPRITE_SIZE = 128 -->
<!-- CEILING_H = 50, GROUND_Y = 750 -->
<!-- DIR_UP=1, DIR_DOWN=2, DIR_LEFT=3, DIR_RIGHT=4, DIR_POSE=5, DIR_IDLE=6 -->

<!-- New constants to append (exact names and values — per D-01, D-02, D-03): -->
<!-- PLAYER_SPEED_UP = 200    # px/s — rising against gravity -->
<!-- PLAYER_SPEED_H = 300     # px/s — horizontal -->
<!-- PLAYER_SPEED_DOWN = 400  # px/s — falling with gravity -->
<!-- ANIM_INTERVAL = 0.15     # seconds per animation frame (~7 Hz) -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Append Phase 2 movement constants to settings.py</name>
  <files>settings.py</files>
  <read_first>
    settings.py (read the full file to find the correct append point — add after the Direction constants block)
    .planning/phases/02-two-characters-move/02-CONTEXT.md (D-01, D-02, D-03 — exact constant names and values)
    CLAUDE.md ("All geometry constants in settings.py — never hardcode pixel values")
  </read_first>
  <action>
    Open settings.py and append the following block at the very end of the file. Do NOT modify any existing constants. Do NOT add any executable code.

    Append exactly this block:

      # Player Movement (Phase 2)
      PLAYER_SPEED_UP = 200      # px/s — rising against gravity (D-01)
      PLAYER_SPEED_H = 300       # px/s — horizontal left and right (D-01)
      PLAYER_SPEED_DOWN = 400    # px/s — falling with gravity (D-01)
      ANIM_INTERVAL = 0.15       # seconds per animation frame (~7 Hz) (D-03)

    These four constants are shared by both Superman and Goblin (D-02). ANIM_INTERVAL governs both air sprite alternation and ground walk cadence.

    After appending, the file must still be importable with no errors and all existing Phase 1 constants must remain unchanged.
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, '.'); import settings; assert settings.PLAYER_SPEED_UP == 200; assert settings.PLAYER_SPEED_H == 300; assert settings.PLAYER_SPEED_DOWN == 400; assert settings.ANIM_INTERVAL == 0.15; assert settings.SCREEN_W == 1280; assert settings.GROUND_Y == 750; print('settings phase2 OK')"</automated>
  </verify>
  <done>settings.py has all four new constants at the correct values. All existing Phase 1 constants are unchanged. The python -c assertion above exits 0 and prints "settings phase2 OK".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings.py → all modules | Movement speed constants flow into player.py; wrong values affect gameplay feel but not security |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | settings.py constants | accept | Local dev file only; no user input; values are visible in source and validated by automated verify command |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After task completes:

  python -c "import settings; print(settings.PLAYER_SPEED_UP, settings.PLAYER_SPEED_H, settings.PLAYER_SPEED_DOWN, settings.ANIM_INTERVAL)"
  Expected output: 200 300 400 0.15

  python -c "import settings; print(settings.SCREEN_W, settings.GROUND_Y, settings.DIR_LEFT)"
  Expected output: 1280 750 3
  (Confirms existing constants are untouched)
</verification>

<success_criteria>
- settings.PLAYER_SPEED_UP == 200
- settings.PLAYER_SPEED_H == 300
- settings.PLAYER_SPEED_DOWN == 400
- settings.ANIM_INTERVAL == 0.15
- All Phase 1 constants (SCREEN_W, SCREEN_H, FPS, SPRITE_SIZE, CEILING_H, GROUND_Y, DIR_*, colors, BEAM_SPEED, WIN_SCORE) are unchanged
- settings.py remains free of executable code — constants only
</success_criteria>

<output>
Create .planning/phases/02-two-characters-move/02-01-SUMMARY.md when done
</output>
