---
phase: 04-death-respawn-scoring
plan: 04
type: execute
wave: 3
depends_on:
  - "04-02"
  - "04-03"
files_modified:
  - game.py
autonomous: true
requirements:
  - DTH-05
  - DTH-06
  - SCR-02
  - SCR-03
  - SCR-04
  - SCR-05
  - SCR-06

must_haves:
  truths:
    - "game.py imports draw_hud from hud and WIN_SCORE from settings"
    - "Game.__init__ has self.osg_score = 0 and self._hud_font"
    - "K_UP respawns Superman when DEAD (state-gated)"
    - "K_w respawns Goblin when DEAD (state-gated)"
    - "MOUSEBUTTONDOWN on a living player starts crash and increments osg_score"
    - "Beam hit calls player.start_crash() and increments opponent's hit_bonus by 10"
    - "Idle-airborne ALIVE players accumulate raw_pose += 1 each frame"
    - "Win condition: any player.score >= WIN_SCORE or osg_score >= WIN_SCORE sets GAME_OVER"
    - "draw_hud() called each frame before pygame.display.flip()"
  artifacts:
    - path: "game.py"
      provides: "Full Phase 4 game loop wiring — respawn, scoring, HUD, win condition"
      contains: "draw_hud"
  key_links:
    - from: "game.py"
      to: "hud.py"
      via: "from hud import draw_hud"
      pattern: "from hud import"
    - from: "game.py"
      to: "player.py"
      via: "player.start_crash(), player.respawn(), player.raw_pose, player.hit_bonus, player.score"
      pattern: "player.start_crash"
---

<objective>
Wire Phase 4 features into game.py: respawn key handlers, mouse-click crash, pose score accumulation, beam-hit scoring, win condition, and HUD rendering. game.py already has collision detection; this plan upgrades it to call start_crash() and award hit_bonus.

Purpose: ties together all Phase 4 components — player.py (04-02) and hud.py (04-03) are complete prerequisites.
Output: game.py with fully wired Phase 4 loop
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/04-death-respawn-scoring/04-CONTEXT.md
@game.py
@settings.py
</context>

<interfaces>
<!-- Imports to update at top of game.py: -->
<!--   from hud import draw_hud                                   (new) -->
<!--   from settings import (..., WIN_SCORE)                     (add WIN_SCORE to existing import) -->

<!-- Game.__init__ additions (after self.players = [...]): -->
<!--   self.osg_score: int = 0                                   # OmnipotentShootingGuy click score (SCR-04) -->
<!--   self._hud_font = pygame.font.Font(None, 28)               # HUD font (SCR-05) -->

<!-- run() KEYDOWN additions (inside the existing elif event.type == pygame.KEYDOWN block): -->
<!--   elif event.key == pygame.K_UP:                            # Superman respawn (DTH-06) -->
<!--       sup = self.players[0] -->
<!--       if sup.state == CharState.DEAD: -->
<!--           sup.respawn() -->
<!--   elif event.key == pygame.K_w:                             # Goblin respawn (DTH-06) -->
<!--       gob = self.players[1] -->
<!--       if gob.state == CharState.DEAD: -->
<!--           gob.respawn() -->

<!-- run() MOUSEBUTTONDOWN handler (new elif after KEYDOWN block): -->
<!--   elif event.type == pygame.MOUSEBUTTONDOWN:                # OSG click (SCR-04) -->
<!--       for player in self.players: -->
<!--           if player.state == CharState.ALIVE and player.rect.collidepoint(event.pos): -->
<!--               player.start_crash() -->
<!--               self.osg_score += 1 -->

<!-- update() collision change: replace "player.state = CharState.CRASHING" with: -->
<!--   player.start_crash()                                      (DTH-01) -->
<!--   self.players[1 - i].hit_bonus += 10                      (SCR-03) -->

<!-- update() pose accumulation (append at end of update(), after beam update loop): -->
<!--   for player in self.players: -->
<!--       if (player.state == CharState.ALIVE -->
<!--               and player._direction == DIR_IDLE -->
<!--               and not player._on_ground): -->
<!--           player.raw_pose += 1                             (SCR-02) -->

<!-- update() win condition (append after pose accumulation): -->
<!--   if self.state == GameState.PLAYING: -->
<!--       for player in self.players: -->
<!--           if player.score >= WIN_SCORE: -->
<!--               self.state = GameState.GAME_OVER -->
<!--       if self.osg_score >= WIN_SCORE: -->
<!--           self.state = GameState.GAME_OVER -->

<!-- draw() change: add draw_hud call before pygame.display.flip(): -->
<!--   draw_hud(self.screen, self.players, self.osg_score, self._hud_font) -->
<!--   pygame.display.flip() -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire Phase 4 features into game.py</name>
  <files>game.py</files>
  <read_first>
    game.py (full file — understand all existing code before making any changes)
    .planning/phases/04-death-respawn-scoring/04-CONTEXT.md (D-12 through D-21 — scoring, HUD, win, respawn)
    settings.py (confirm WIN_SCORE importable; confirm DIR_IDLE present)
    CLAUDE.md (KEYDOWN for one-shot actions; no hardcoded pixel values)
  </read_first>
  <action>
    Make the minimum changes to game.py to wire all Phase 4 features. Do NOT refactor existing code.

    CHANGE 1 — Add hud import at the top (after existing imports):
      from hud import draw_hud

    CHANGE 2 — Extend settings import to include WIN_SCORE and DIR_IDLE:
      from settings import (
          SCREEN_W,
          SCREEN_H,
          CEILING_H,
          GROUND_Y,
          SKY_COLOR,
          CEILING_COLOR,
          GROUND_COLOR,
          FPS,
          DIR_LEFT,
          DIR_RIGHT,
          DIR_IDLE,
          WIN_SCORE,
      )

    CHANGE 3 — In Game.__init__, AFTER self.players = [...], append:
          self.osg_score: int = 0
          self._hud_font = pygame.font.Font(None, 28)

    CHANGE 4 — In run(), inside the KEYDOWN block, AFTER the K_e branch (Goblin fire), add:
                    elif event.key == pygame.K_UP:
                        sup = self.players[0]
                        if sup.state == CharState.DEAD:
                            sup.respawn()
                    elif event.key == pygame.K_w:
                        gob = self.players[1]
                        if gob.state == CharState.DEAD:
                            gob.respawn()

    CHANGE 5 — In run(), AFTER the entire KEYDOWN elif block, add a new event type handler:
                elif event.type == pygame.MOUSEBUTTONDOWN:
                    for player in self.players:
                        if player.state == CharState.ALIVE and player.rect.collidepoint(event.pos):
                            player.start_crash()
                            self.osg_score += 1

    CHANGE 6 — In update(), in the collision detection block, replace:
          player.state = CharState.CRASHING
      with:
          player.start_crash()
          self.players[1 - i].hit_bonus += 10

    CHANGE 7 — In update(), AFTER the beam update loop (after the player.beams = deque(...) block),
      append pose accumulation:
          # Pose score: +1 per frame idle-airborne (SCR-02)
          for player in self.players:
              if (player.state == CharState.ALIVE
                      and player._direction == DIR_IDLE
                      and not player._on_ground):
                  player.raw_pose += 1

    CHANGE 8 — In update(), AFTER the pose accumulation block, append win condition:
          # Win condition (SCR-06)
          if self.state == GameState.PLAYING:
              for player in self.players:
                  if player.score >= WIN_SCORE:
                      self.state = GameState.GAME_OVER
              if self.osg_score >= WIN_SCORE:
                  self.state = GameState.GAME_OVER

    CHANGE 9 — In draw(), BEFORE pygame.display.flip(), insert:
          draw_hud(self.screen, self.players, self.osg_score, self._hud_font)

    CORRECTNESS NOTES:
    - K_UP is also Superman's movement key (get_pressed), but here it's KEYDOWN and state-gated to DEAD — no conflict
    - K_w is also Goblin's movement key (get_pressed), but here it's KEYDOWN and state-gated to DEAD — no conflict
    - Collision: `self.players[1 - i].hit_bonus += 10` scores the shooter (opponent of the hit player at index i)
    - Pose accumulation reads player._direction and player._on_ground — these are set by player.update() which runs before
    - Win condition check must guard with `self.state == GameState.PLAYING` to avoid repeat transitions
    - update() and draw() should still run even in GAME_OVER (freeze frame). No state guard needed on update().
  </action>
  <verify>
    <automated>python -c "
import sys, os
os.environ['SDL_VIDEODRIVER'] = 'dummy'
os.environ['SDL_AUDIODRIVER'] = 'dummy'
sys.path.insert(0, '.')
import pygame
pygame.init()
screen = pygame.display.set_mode((1280, 800))
from unittest.mock import MagicMock, patch
from collections import defaultdict

assets = MagicMock()
assets.sprites = defaultdict(lambda: pygame.Surface((128, 128)))

from game import Game, GameState
from player import Player, CharState
from settings import WIN_SCORE, DIR_IDLE, CEILING_STOP_Y, GROUND_STOP_Y

g = Game(screen, assets)

# osg_score and _hud_font exist
assert hasattr(g, 'osg_score') and g.osg_score == 0
assert hasattr(g, '_hud_font')

# draw_hud is wired (draw() runs without error)
g.draw()

# Collision now uses start_crash and awards hit_bonus
from beam import Beam
from settings import DIR_LEFT
sup = g.players[0]
gob = g.players[1]

# Position Goblin directly in beam path of Superman
gob.x = sup.x - 50
gob.y = sup.y
pre_hit = gob.hit_bonus  # should be 0

# fire a beam from Goblin toward Superman
gob_beam = Beam(gob.x + 54, gob.y + 54, DIR_LEFT, (0, 200, 0))
gob.beams.append(gob_beam)
# Manually place Superman in beam path
sup.x = gob.x - 60
sup.y = gob.y

# Simulate collision for Superman (player index 0 vs goblin beams at index 1)
# Manually call update
g.update(0.016)
# Either start_crash was called or beam missed; test the path exists by checking import
from game import Game as G2
import inspect
src = inspect.getsource(G2.update)
assert 'start_crash' in src, 'start_crash not found in update()'
assert 'hit_bonus' in src, 'hit_bonus not found in update()'
assert 'raw_pose' in src, 'raw_pose not found in update()'
assert 'WIN_SCORE' in src, 'WIN_SCORE not found in update()'
assert 'draw_hud' in inspect.getsource(G2.draw), 'draw_hud not found in draw()'

# respawn keys wired: set superman DEAD then trigger K_UP event
sup2 = g.players[0]
sup2.state = CharState.DEAD
sup2.y = float(GROUND_STOP_Y)
event = pygame.event.Event(pygame.KEYDOWN, key=pygame.K_UP, mod=0, unicode='', scancode=0)
pygame.event.post(event)
# Process one event loop iteration
for ev in pygame.event.get():
    if ev.type == pygame.KEYDOWN and ev.key == pygame.K_UP:
        if g.players[0].state == CharState.DEAD:
            g.players[0].respawn()
assert g.players[0].state == CharState.ALIVE, 'Superman should be ALIVE after K_UP respawn'

# win condition: set score high and run update
g.players[0].hit_bonus = WIN_SCORE
g.update(0.016)
assert g.state == GameState.GAME_OVER, f'Expected GAME_OVER, got {g.state}'

print('game.py phase4 OK')
pygame.quit()
"</automated>
  </verify>
  <done>game.py has all 9 changes. Verify exits 0 and prints "game.py phase4 OK". python -m pytest tests/ -v exits 0 (no regressions).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| game.py → pygame.MOUSEBUTTONDOWN | event.pos from OS; player.rect.collidepoint() bounds-checks it — no injection surface |
| game.py → player.score | Read-only property; values bounded by game duration and WIN_SCORE |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | Score overflow | accept | raw_pose and hit_bonus grow only via game events; no external input path |
| T-04-05 | Tampering | Respawn key conflict | accept | state-gating (CharState.DEAD) prevents movement-key collision |
| T-04-SC | Tampering | pip installs | accept | No new packages |
</threat_model>

<verification>
  python -c "from game import Game; print('import OK')"
  Expected: import OK

  Full verify (above) exits 0 and prints "game.py phase4 OK"

  Regression:
    python -m pytest tests/ -v
    Expected: all prior tests pass
</verification>

<success_criteria>
- from hud import draw_hud present in game.py
- WIN_SCORE and DIR_IDLE in settings import
- self.osg_score = 0 and self._hud_font in Game.__init__
- K_UP KEYDOWN respawns Superman when state == DEAD
- K_w KEYDOWN respawns Goblin when state == DEAD
- MOUSEBUTTONDOWN on a living player calls start_crash() and increments osg_score
- Collision calls player.start_crash() (not direct state assignment) and awards hit_bonus += 10
- Idle-airborne ALIVE players get raw_pose += 1 each frame
- Win condition checked each update: score >= WIN_SCORE or osg_score >= WIN_SCORE → GAME_OVER
- draw_hud() called before pygame.display.flip()
</success_criteria>

<output>
Create .planning/phases/04-death-respawn-scoring/04-04-SUMMARY.md when done
</output>
