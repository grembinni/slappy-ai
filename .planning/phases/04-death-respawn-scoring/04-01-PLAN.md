---
phase: 04-death-respawn-scoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - settings.py
autonomous: true
requirements:
  - DTH-03

must_haves:
  truths:
    - "settings.SPIN_INTERVAL == 0.1"
  artifacts:
    - path: "settings.py"
      provides: "Phase 4 crash animation timing constant"
      contains: "SPIN_INTERVAL = 0.1"
  key_links:
    - from: "settings.py"
      to: "player.py"
      via: "from settings import SPIN_INTERVAL"
      pattern: "from settings import"
---

<objective>
Append one Phase 4 crash animation constant to settings.py: SPIN_INTERVAL = 0.1 (100ms per spin frame, matching DTH-03). All geometry and timing constants live in settings.py per CLAUDE.md.

Purpose: player.py (Plan 04-02) imports SPIN_INTERVAL for the spin cycle timer. Must exist first.
Output: settings.py extended with SPIN_INTERVAL = 0.1
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/04-death-respawn-scoring/04-CONTEXT.md
@settings.py
</context>

<interfaces>
<!-- Existing settings.py constants (do NOT change these) -->
<!-- New constant to append: -->
<!-- SPIN_INTERVAL = 0.1   # seconds per spin frame during crash (DTH-03) -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Append SPIN_INTERVAL to settings.py</name>
  <files>settings.py</files>
  <read_first>
    settings.py (full file — find the correct append point)
    .planning/phases/04-death-respawn-scoring/04-CONTEXT.md (D-02: 100ms spin cadence)
    CLAUDE.md ("All geometry constants in settings.py — never hardcode pixel values")
  </read_first>
  <action>
    Append exactly this block at the end of settings.py under an existing or new Phase 4 comment:

      # Death & Crash (Phase 4)
      SPIN_INTERVAL = 0.1    # seconds per spin frame during crash animation (DTH-03)

    Do NOT modify any existing constants. Do NOT add executable code.
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, '.'); import settings; assert settings.SPIN_INTERVAL == 0.1; assert settings.BEAM_SPEED == 800; assert settings.SCREEN_W == 1280; print('settings phase4 OK')"</automated>
  </verify>
  <done>settings.py has SPIN_INTERVAL == 0.1. All existing constants unchanged. Verify command exits 0 and prints "settings phase4 OK".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings.py → player.py | Timing constant; wrong value changes animation feel but not security |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | settings.py constants | accept | Local dev file only; validated by automated verify |
| T-04-SC | Tampering | pip installs | accept | No new packages |
</threat_model>

<verification>
  python -c "import settings; print(settings.SPIN_INTERVAL)"
  Expected: 0.1
</verification>

<success_criteria>
- settings.SPIN_INTERVAL == 0.1
- All prior constants unchanged
- settings.py remains free of executable code
</success_criteria>

<output>
Create .planning/phases/04-death-respawn-scoring/04-01-SUMMARY.md when done
</output>
