---
phase: 01-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - raw_assets/icons/.gitkeep
  - raw_assets/sounds/.gitkeep
autonomous: false
requirements:
  - ENG-05

must_haves:
  truths:
    - "raw_assets/icons/ exists and contains exactly 40 ICO files per the RESEARCH.md manifest"
    - "raw_assets/sounds/ exists and contains LASER.WAV, DeathCry.WAV, EXPLODE.WAV, Intro.wav, passport.mid, canyon.mid"
    - "raw_assets/ is committed to the repo (not gitignored)"
    - "Excluded files (LITENING.ICO, PLANE.ICO, ROCKET.ICO, Cloud*.ico, etc.) are NOT present"
  artifacts:
    - path: "raw_assets/icons/"
      provides: "Source ICO sprites (40 files)"
      contains: "GEvil.ico"
    - path: "raw_assets/sounds/"
      provides: "Source WAV/MIDI files (6 files)"
      contains: "passport.mid"
  key_links:
    - from: "raw_assets/icons/*.ico"
      to: "assets/sprites/*.png"
      via: "convert_assets.py ICO→PNG pipeline"
      pattern: "raw_assets/icons"

user_setup:
  - service: raw_assets population
    why: "ICO and sound files must be copied from the local VB6 source directory — this is a manual file copy that cannot be automated because the source path is local-only"
    steps:
      - task: "Create raw_assets/ directory structure"
        detail: "mkdir raw_assets\\icons raw_assets\\sounds (if not already created)"
      - task: "Copy 40 ICO files to raw_assets/icons/"
        detail: "Copy from D:\\dev\\repo\\best-game-ever\\_old\\Icons\\ — see exact file list below"
      - task: "Copy 6 sound files to raw_assets/sounds/"
        detail: "Copy from D:\\dev\\repo\\best-game-ever\\_old\\Sounds\\ — see exact file list below"
---

<objective>
Create raw_assets/ directory structure and populate it with the 40 ICO sprite files and 6 sound/MIDI files required by the RESEARCH.md manifest. This is a human-action checkpoint because the source files live at a local path (D:\dev\repo\best-game-ever\_old\) that cannot be referenced in committed plans (D-01).

Purpose: convert_assets.py (Plan 04) reads from raw_assets/ — it cannot run until raw_assets/ is populated.
Output: raw_assets/icons/ (40 ICO files), raw_assets/sounds/ (6 files), both committed to git.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create raw_assets/ directory structure and .gitkeep files</name>
  <files>raw_assets/icons/.gitkeep, raw_assets/sounds/.gitkeep</files>
  <read_first>
    .planning/phases/01-foundation/01-CONTEXT.md (D-01: raw_assets/ committed to repo; D-02: exclusion list)
    .planning/phases/01-foundation/01-RESEARCH.md (Raw Assets Manifest section — complete file list with exclusions)
    .gitignore (verify raw_assets/ is NOT in the ignore list — it must not be gitignored)
  </read_first>
  <action>
    Create the directory structure:
      raw_assets/
        icons/
        sounds/

    Create empty placeholder files raw_assets/icons/.gitkeep and raw_assets/sounds/.gitkeep so git tracks the empty directories before the human copy step.

    Verify .gitignore does NOT contain raw_assets/ — if it does, that is a bug from Plan 01 and must be corrected. raw_assets/ is committed source (per D-01); only assets/ is gitignored.

    Do NOT create any ICO or WAV files — those are populated by the human in Task 2 (checkpoint).
  </action>
  <verify>
    <automated>python -c "import pathlib; assert pathlib.Path('raw_assets/icons/.gitkeep').exists(); assert pathlib.Path('raw_assets/sounds/.gitkeep').exists(); gi=pathlib.Path('.gitignore').read_text(); assert 'raw_assets' not in gi; print('raw_assets structure OK')"</automated>
  </verify>
  <done>raw_assets/icons/ and raw_assets/sounds/ exist with .gitkeep files. .gitignore does not exclude raw_assets/.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Human copies source files to raw_assets/</name>
  <what-built>Directory structure at raw_assets/icons/ and raw_assets/sounds/ is ready to receive files.</what-built>
  <how-to-verify>
    Copy the following files from your local VB6 source directory to raw_assets/.

    SOURCE: D:\dev\repo\best-game-ever\_old\Icons\
    DESTINATION: raw_assets\icons\

    Copy EXACTLY these 40 ICO files (match case as shown):
      Superman sprites (20 files):
        SUp1.ico, SUp2.ico
        SDown1.ico, SDown2.ico
        SLeft1.ICO, SLeft2.ico
        SRight1.ico, SRight2.ico
        SPose.ico
        SDeath.ico
        SSpin1.ico, SSpin2.ico, SSpin3.ico, SSpin4.ico
        CKent.ico, CKent1.ico, CKent2.ico, CKent3.ico
        CKentIntro.ico
        Superman.ico

      Goblin sprites (20 files):
        GUp1.ico, GUp2.ico
        GDown1.ico, GDown2.ico
        GLeft1.ico, GLeft2.ico
        GRight1.ico, GRight2.ico
        GEvil.ico, GEvil1.ico, GEvil2.ico, GEvil3.ico
        GEvilIntro.ico
        GDeath.ico
        GPose.ico, GPose2.ico
        GSpin1.ico, GSpin2.ico, GSpin3.ico, GSpin4.ico

    DO NOT copy: LITENING.ICO, PLANE.ICO, ROCKET.ICO, Cloud.ico, Cloud2.ico, Cloud3.ico,
      Hobgoblin.ico, Hobgoblin2.ico, tree.ico, HobGoblin.cur, tree.bmp, bit.bmp, e.bmp,
      SDown.ico, SRight.ico, SDown3.ico, SDownBack.ico, SDownFront.ico, SLDown3.ico, SUp.ico
      (These are excluded per D-02 — they are unused in the game or are duplicates.)

    SOURCE: D:\dev\repo\best-game-ever\_old\Sounds\
    DESTINATION: raw_assets\sounds\

    Copy EXACTLY these 6 files:
      LASER.WAV
      DeathCry.WAV
      EXPLODE.WAV
      Intro.wav
      passport.mid
      canyon.mid

    DO NOT copy: APPLAUSE.WAV, BOILGUY2.WAV, CLAP.WAV, DEADGUY4.WAV, FF_BATL.WAV,
      FF_MSG.WAV, GUNSHOT.WAV, WHOOSH.WAV
      (These are excluded per D-02 — unused in v1 requirements.)

    After copying, run this verification command to confirm the count:
      python -c "import pathlib; icons=list(pathlib.Path('raw_assets/icons').glob('*.ico')) + list(pathlib.Path('raw_assets/icons').glob('*.ICO')); sounds=list(pathlib.Path('raw_assets/sounds').iterdir()); print(f'Icons: {len(icons)}, Sounds: {len(sounds)}'); assert len(icons)==40, f'Expected 40 ICO files, got {len(icons)}'; assert len(sounds)==6, f'Expected 6 sound files, got {len(sounds)}'"

    Expected output: Icons: 40, Sounds: 6 (or close — file counts include .gitkeep; adjust if needed)

    Note: .gitkeep files in the directories will not match *.ico glob, so the count should be accurate.
  </how-to-verify>
  <resume-signal>Type "copied" after the verification command prints "Icons: 40, Sounds: 6" without assertion errors.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local filesystem → raw_assets/ | Source files copied from developer's local path; no network, no external download |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | ICO file contents | accept | Files copied from known local VB6 repo; no external source; Pillow raises UnidentifiedImageError if any ICO is malformed — handled in Plan 04 |
| T-01-06 | Spoofing | Wrong files in raw_assets/ | mitigate | Count assertion in checkpoint verification step confirms exactly 40 ICO + 6 sound files; excluded files are explicitly listed |
| T-01-SC | Tampering | git commit of raw_assets/ | accept | Files are pre-existing VB6 game assets on local dev machine; no external supply chain risk |
</threat_model>

<verification>
After human copy completes:
  python -c "import pathlib; icons=[f for f in pathlib.Path('raw_assets/icons').iterdir() if f.suffix.lower()=='.ico']; sounds=[f for f in pathlib.Path('raw_assets/sounds').iterdir() if not f.name.startswith('.')]; print(f'ICO files: {len(icons)}'); print(f'Sound files: {len(sounds)}'); assert len(icons)==40; assert len(sounds)==6"
  Expected: ICO files: 40 / Sound files: 6

  python -c "import pathlib; assert pathlib.Path('raw_assets/icons/GEvil.ico').exists(); assert pathlib.Path('raw_assets/sounds/passport.mid').exists(); print('Key files present')"
  Expected: Key files present
</verification>

<success_criteria>
- raw_assets/icons/ contains exactly 40 ICO files matching the RESEARCH.md manifest
- raw_assets/sounds/ contains exactly LASER.WAV, DeathCry.WAV, EXPLODE.WAV, Intro.wav, passport.mid, canyon.mid
- Excluded files (LITENING.ICO, PLANE.ICO, ROCKET.ICO, Cloud*.ico, unused WAVs) are NOT present
- raw_assets/ is NOT gitignored
- Verification count assertion exits 0 with "Icons: 40, Sounds: 6"
</success_criteria>

<output>
Create .planning/phases/01-foundation/01-03-SUMMARY.md when done
</output>
