---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_settings.py
  - tests/test_convert.py
autonomous: true
requirements:
  - ENG-01
  - ENG-02
  - ENG-04
  - ENG-05

must_haves:
  truths:
    - "pytest discovers all tests in tests/ directory"
    - "test_settings.py tests ENG-01: FPS constant importable and equals 60"
    - "test_settings.py tests ENG-02: GameState enum has SPLASH, PLAYING, PAUSED, GAME_OVER members"
    - "test_settings.py tests ENG-04: all six constants (SCREEN_W, SCREEN_H, SPRITE_SIZE, WIN_SCORE, BEAM_SPEED, FPS) importable with correct values"
    - "test_convert.py tests ENG-05: dry-run mode lists expected ICO and MIDI files without writing to disk"
    - "conftest.py provides shared fixtures for tests"
  artifacts:
    - path: "tests/conftest.py"
      provides: "Shared fixtures"
      exports: ["assets_dir", "raw_assets_dir"]
    - path: "tests/test_settings.py"
      provides: "Constants verification tests"
      contains: "def test_constants"
    - path: "tests/test_convert.py"
      provides: "convert_assets.py dry-run test"
      contains: "def test_dry_run"
  key_links:
    - from: "tests/test_settings.py"
      to: "settings.py"
      via: "from settings import ..."
      pattern: "from settings import"
    - from: "tests/test_convert.py"
      to: "convert_assets.py"
      via: "import convert_assets"
      pattern: "import convert_assets"
---

<objective>
Create the test scaffold for ENG-01, ENG-02, ENG-04, and ENG-05. Tests are written before or alongside the production code they verify (TDD orientation where applicable). test_assets.py is deferred to Plan 05 because AssetCache requires assets/ to exist (convert_assets.py must run first).

Purpose: Nyquist rule — every behavior gets a test. Writing tests now prevents the "test later" anti-pattern. These tests run green immediately after Plan 01 (settings.py) and Plan 04 (convert_assets.py) are complete.
Output: tests/conftest.py, tests/test_settings.py, tests/test_convert.py, tests/__init__.py
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<interfaces>
<!-- settings.py constants that test_settings.py must verify — exact values from RESEARCH.md Pattern 4 -->
<!-- From settings.py (created by Plan 01): -->
SCREEN_W = 1280
SCREEN_H = 800
FPS = 60
SPRITE_SIZE = 128
WIN_SCORE = 50
BEAM_SPEED = 400

<!-- From game.py (created by Plan 05) — GameState enum for ENG-02 test: -->
<!-- class GameState(Enum): SPLASH = auto(); PLAYING = auto(); PAUSED = auto(); GAME_OVER = auto() -->

<!-- convert_assets.py --dry-run flag (created by Plan 04): -->
<!-- python convert_assets.py --dry-run  → prints file list, exits 0, writes nothing to disk -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Test infrastructure — conftest.py and __init__.py</name>
  <files>tests/__init__.py, tests/conftest.py</files>
  <read_first>
    .planning/phases/01-foundation/01-VALIDATION.md (Test Framework section — pytest.ini already created in Plan 01)
    .planning/phases/01-foundation/01-RESEARCH.md (Architecture Patterns — ASSETS_DIR path convention)
  </read_first>
  <action>
    Create tests/__init__.py as an empty file (allows pytest to discover the package).

    Create tests/conftest.py with these shared fixtures:

    1. assets_dir fixture — returns pathlib.Path to the assets/ directory at the project root:
       @pytest.fixture
       def assets_dir():
           return pathlib.Path(__file__).parent.parent / "assets"

    2. raw_assets_dir fixture — returns pathlib.Path to raw_assets/ at the project root:
       @pytest.fixture
       def raw_assets_dir():
           return pathlib.Path(__file__).parent.parent / "raw_assets"

    3. sprites_dir fixture — returns assets_dir / "sprites":
       @pytest.fixture
       def sprites_dir(assets_dir):
           return assets_dir / "sprites"

    4. sounds_dir fixture — returns assets_dir / "sounds":
       @pytest.fixture
       def sounds_dir(assets_dir):
           return assets_dir / "sounds"

    Import pathlib and pytest at the top. No other content.
  </action>
  <verify>
    <automated>.\venv\Scripts\python -m pytest tests/ --collect-only -q</automated>
  </verify>
  <done>tests/__init__.py exists (empty). tests/conftest.py exists with four fixtures: assets_dir, raw_assets_dir, sprites_dir, sounds_dir. pytest --collect-only runs without import errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test_settings.py — ENG-01, ENG-02, ENG-04 coverage</name>
  <files>tests/test_settings.py</files>
  <read_first>
    settings.py (the file being tested — read its exact constant values before writing assertions)
    .planning/REQUIREMENTS.md (ENG-01, ENG-02, ENG-04 requirement text — each assertion must trace to one of these)
    .planning/phases/01-foundation/01-VALIDATION.md (Phase Requirements → Test Map — function names specified there)
  </read_first>
  <behavior>
    - test_fps_constant: settings.FPS == 60 and isinstance(settings.FPS, int)  [ENG-01]
    - test_gamestate_enum: GameState has exactly the four members SPLASH, PLAYING, PAUSED, GAME_OVER [ENG-02]
    - test_constants: SCREEN_W==1280, SCREEN_H==800, SPRITE_SIZE==128, WIN_SCORE==50, BEAM_SPEED==400, FPS==60 [ENG-04]
    - test_zone_constants: CEILING_H==50, GROUND_Y==750 [ENG-04 by extension — all geometry constants]
    - test_colors: SKY_COLOR==(0,255,255), CEILING_COLOR==(255,255,255), GROUND_COLOR==(0,128,0) [ENG-04]
    - test_direction_constants: DIR_UP==1, DIR_DOWN==2, DIR_LEFT==3, DIR_RIGHT==4, DIR_POSE==5, DIR_IDLE==6 [ENG-04]
    - test_beam_colors: GOBLIN_BEAM_COLOR==(0,200,0), SUPERMAN_BEAM_COLOR==(200,0,0) [ENG-04]
  </behavior>
  <action>
    Create tests/test_settings.py.

    Import block: import pytest, import sys, sys.path.insert(0, str(pathlib.Path(__file__).parent.parent)) to ensure project root is on path.

    For ENG-01 (test_fps_constant): import settings; assert settings.FPS == 60; assert isinstance(settings.FPS, int). Also assert settings.FPS > 0 and settings.FPS <= 120 (sane range).

    For ENG-02 (test_gamestate_enum): import game.GameState or from game import GameState. NOTE: game.py does not exist yet (created in Plan 05). Write this test but mark it with @pytest.mark.skip(reason="game.py created in Plan 05") so the full test suite doesn't fail before game.py exists. The skip marker must be removed when Plan 05 is complete.

    For ENG-04 (test_constants): assert each of the six constants specified in the requirement — SCREEN_W, SCREEN_H, SPRITE_SIZE, WIN_SCORE, BEAM_SPEED, FPS — against exact values. Use individual assertions with clear variable names, not a dict loop.

    For test_zone_constants: assert CEILING_H == 50, GROUND_Y == 750. Also assert GROUND_Y + (SCREEN_H - GROUND_Y) == SCREEN_H (i.e., the ground band reaches the bottom).

    For test_colors: assert each tuple element individually so failures report which channel is wrong. E.g.: assert settings.SKY_COLOR[0] == 0; assert settings.SKY_COLOR[1] == 255; assert settings.SKY_COLOR[2] == 255.

    For test_direction_constants: assert all six direction constants against their integer values.

    For test_beam_colors: assert GOBLIN_BEAM_COLOR == (0, 200, 0) and SUPERMAN_BEAM_COLOR == (200, 0, 0).

    Function names must exactly match what VALIDATION.md specifies: test_fps_constant, test_gamestate_enum, test_constants. Additional tests may use any descriptive name.
  </action>
  <verify>
    <automated>.\venv\Scripts\python -m pytest tests/test_settings.py::test_fps_constant tests/test_settings.py::test_constants -x -q</automated>
  </verify>
  <done>tests/test_settings.py exists. All tests except test_gamestate_enum pass immediately (test_gamestate_enum is marked @pytest.mark.skip until Plan 05 creates game.py). pytest tests/test_settings.py -x -q exits 0 with all non-skipped tests passing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: test_convert.py — ENG-05 dry-run coverage</name>
  <files>tests/test_convert.py</files>
  <read_first>
    .planning/REQUIREMENTS.md (ENG-05 requirement text — "converts .ico sprites to .png and .mid music to .wav")
    .planning/phases/01-foundation/01-VALIDATION.md (ENG-05 row — dry-run test approach)
    .planning/phases/01-foundation/01-RESEARCH.md (Raw Assets Manifest section — 40 ICO files + 6 sound files listed; convert_assets.py CLI structure)
  </read_first>
  <behavior>
    - test_dry_run: running convert_assets.main(dry_run=True) or calling the script with --dry-run exits 0 and produces NO files in assets/
    - test_dry_run_output: --dry-run stdout includes at least the strings "GEvil.ico" and "passport.mid" (confirming the manifest is being read)
    - test_skip_midi_flag: --skip-midi flag causes only ICO→PNG to be attempted; MIDI files are skipped without sys.exit(1)
    - All tests skip if convert_assets.py does not exist yet (Plan 04 creates it)
  </behavior>
  <action>
    Create tests/test_convert.py.

    At the top, after imports, add a module-level skip guard:
      import pathlib
      CONVERT_PATH = pathlib.Path(__file__).parent.parent / "convert_assets.py"
      pytestmark = pytest.mark.skipif(not CONVERT_PATH.exists(), reason="convert_assets.py not yet created (Plan 04)")

    This marks the entire module as skip if convert_assets.py doesn't exist. When Plan 04 runs, these tests will automatically become active.

    test_dry_run: Use subprocess.run to call ["python", "convert_assets.py", "--dry-run"] with cwd set to project root. Assert returncode == 0. Assert that assets/sprites/ does NOT have any newly created .png files (use a temp working dir or check mtime). The simplest approach: verify assets/sprites/ either doesn't exist or had the same mtime before/after the dry-run call. OR: assert the captured stdout contains "--dry-run" or "DRY RUN" indicating dry-run mode was acknowledged.

    test_dry_run_output: Same subprocess call; capture stdout. Assert "GEvil" in stdout (case-insensitive) confirming ICO manifest is walked.

    test_skip_midi_flag: subprocess.run(["python", "convert_assets.py", "--skip-midi", "--dry-run"]). Assert returncode == 0. Assert "passport.mid" not in captured output OR assert the word "skip" or "SKIP" appears in output relating to MIDI.

    Use subprocess.run(..., capture_output=True, text=True, cwd=str(pathlib.Path(__file__).parent.parent)) for all subprocess calls.

    Function name test_dry_run must exactly match what VALIDATION.md specifies.
  </action>
  <verify>
    <automated>.\venv\Scripts\python -m pytest tests/test_convert.py -x -q 2>&1 | Select-Object -First 10</automated>
  </verify>
  <done>tests/test_convert.py exists. All tests in the file are skipped (because convert_assets.py doesn't exist yet) with a clear skip reason. pytest tests/test_convert.py -x -q exits 0 with all tests shown as skipped, not failed. After Plan 04 creates convert_assets.py, these tests will become active.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test subprocess → convert_assets.py | Tests invoke convert_assets.py as a subprocess; the script itself is in-repo and trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | test_convert.py subprocess cwd | accept | cwd is hardcoded to project root derived from __file__; no user input reaches cwd |
| T-01-04 | Information Disclosure | test output capturing | accept | Tests run locally; no sensitive data in stdout/stderr |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; packages were installed at venv setup (Plan 01) |
</threat_model>

<verification>
After all three tasks complete:
  .\venv\Scripts\python -m pytest tests/test_settings.py -x -q
  Expected: all non-skipped tests pass (test_gamestate_enum is skipped); exit 0

  .\venv\Scripts\python -m pytest tests/test_convert.py -x -q
  Expected: all tests skipped with reason "convert_assets.py not yet created (Plan 04)"; exit 0

  .\venv\Scripts\python -m pytest tests/ -v
  Expected: some PASSed, some SKIPped, zero FAILed, zero ERRORed
</verification>

<success_criteria>
- tests/__init__.py, tests/conftest.py, tests/test_settings.py, tests/test_convert.py all exist
- pytest tests/test_settings.py -x -q exits 0 with all non-skipped tests passing
- pytest tests/test_convert.py -x -q exits 0 with all tests skipped (convert_assets.py not yet created)
- Function names test_fps_constant, test_gamestate_enum, test_constants, test_dry_run match VALIDATION.md exactly
- test_gamestate_enum is @pytest.mark.skip until Plan 05 creates game.py
</success_criteria>

<output>
Create .planning/phases/01-foundation/01-02-SUMMARY.md when done
</output>
