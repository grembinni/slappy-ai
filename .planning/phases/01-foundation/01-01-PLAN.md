---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - settings.py
  - requirements.txt
  - requirements-dev.txt
  - .gitignore
  - pytest.ini
autonomous: true
requirements:
  - ENG-04

must_haves:
  truths:
    - "settings.py is importable from the project root with no errors"
    - "All Phase 1 constants are present and have the correct values"
    - "Direction constants are defined in settings.py for use in later phases"
    - "pytest.ini configures test discovery to find tests/ directory"
    - "requirements.txt pins pygame-ce==2.5.7; requirements-dev.txt includes all dev tools"
    - ".gitignore excludes assets/ and common Python artifacts"
  artifacts:
    - path: "settings.py"
      provides: "Single source of truth for all constants"
      contains: "SCREEN_W = 1280"
    - path: "requirements.txt"
      provides: "Runtime dependency list"
      contains: "pygame-ce==2.5.7"
    - path: "requirements-dev.txt"
      provides: "Dev dependency list"
      contains: "Pillow==12.2.0"
    - path: ".gitignore"
      provides: "Git exclusions"
      contains: "assets/"
    - path: "pytest.ini"
      provides: "Test discovery config"
      contains: "testpaths = tests"
  key_links:
    - from: "settings.py"
      to: "game.py, assets.py, player.py, beam.py (future phases)"
      via: "import from settings import SCREEN_W, SCREEN_H, ..."
      pattern: "from settings import"
---

<objective>
Establish the project foundation: all constants in settings.py, project dependency files, .gitignore, and pytest configuration. This is the single source of truth that every subsequent phase imports from — get the values right now.

Purpose: Every other module imports from settings.py. Writing wrong values here causes cascading bugs in later phases.
Output: settings.py (complete Phase 1 constants), requirements.txt, requirements-dev.txt, .gitignore, pytest.ini
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create settings.py with all Phase 1 constants</name>
  <files>settings.py</files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md (Pattern 4: settings.py Constants — exact values and VB6 derivation notes)
    .planning/phases/01-foundation/01-CONTEXT.md (Claude's Discretion section — zone dimensions approved)
    CLAUDE.md (key constraint: "All geometry constants in settings.py — never hardcode pixel values")
  </read_first>
  <behavior>
    - Importing settings gives SCREEN_W == 1280
    - Importing settings gives SCREEN_H == 800
    - Importing settings gives FPS == 60
    - Importing settings gives SPRITE_SIZE == 128
    - Importing settings gives WIN_SCORE == 50
    - Importing settings gives BEAM_SPEED == 400
    - Importing settings gives CEILING_H == 50
    - Importing settings gives GROUND_Y == 750
    - Importing settings gives SKY_COLOR == (0, 255, 255)
    - Importing settings gives CEILING_COLOR == (255, 255, 255)
    - Importing settings gives GROUND_COLOR == (0, 128, 0)
    - Importing settings gives DIR_UP == 1, DIR_DOWN == 2, DIR_LEFT == 3, DIR_RIGHT == 4, DIR_POSE == 5, DIR_IDLE == 6
    - Importing settings gives GOBLIN_BEAM_COLOR == (0, 200, 0) and SUPERMAN_BEAM_COLOR == (200, 0, 0)
  </behavior>
  <action>
    Create settings.py in the project root. Do NOT hardcode any numeric literals in any other file — this is the single exception.

    Define these constants with exactly these names and values:

    Display block: SCREEN_W = 1280, SCREEN_H = 800, FPS = 60

    Sprites block: SPRITE_SIZE = 128 (4x upscale from 32x32 ICO source — the VB6 sprites were ~32x32; sprites.py will load PNGs at this size)

    Background zones block (derived from VB6 frmSuperman.frm — proportional translation documented in RESEARCH.md VB6 Zone Analysis):
      CEILING_H = 50     # white band: y=0 to y=50
      GROUND_Y = 750     # green band starts here: y=750 to y=800 (50px tall)

    Colors block (VB6 source-verified per RESEARCH.md Pattern 4):
      SKY_COLOR = (0, 255, 255)      # VB6 BackColor &H00FFFF00& — OLE BGR: cyan, NOT blue
      CEILING_COLOR = (255, 255, 255) # vbWhite
      GROUND_COLOR = (0, 128, 0)     # vbGreen = 0x008000

    Gameplay block (Phase 3+ use; define now per D-06 intent to establish all needed constants):
      WIN_SCORE = 50
      BEAM_SPEED = 400    # pixels per second [placeholder — tunable in Phase 3]
      GOBLIN_BEAM_COLOR = (0, 200, 0)   # green per REQUIREMENTS.md CMB-04 (VB6 had orange — user chose green)
      SUPERMAN_BEAM_COLOR = (200, 0, 0) # red per REQUIREMENTS.md CMB-04

    Direction constants block (used from Phase 2 onward; define now for import consistency):
      DIR_UP = 1; DIR_DOWN = 2; DIR_LEFT = 3; DIR_RIGHT = 4; DIR_POSE = 5; DIR_IDLE = 6

    Include a comment above each block explaining what it is. Include a derivation note above SKY_COLOR explaining the VB6 OLE BGR decoding. No executable code in settings.py — constants only.
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, '.'); import settings; assert settings.SCREEN_W == 1280; assert settings.SCREEN_H == 800; assert settings.FPS == 60; assert settings.SPRITE_SIZE == 128; assert settings.WIN_SCORE == 50; assert settings.BEAM_SPEED == 400; assert settings.CEILING_H == 50; assert settings.GROUND_Y == 750; assert settings.SKY_COLOR == (0,255,255); assert settings.CEILING_COLOR == (255,255,255); assert settings.GROUND_COLOR == (0,128,0); assert settings.DIR_LEFT == 3; assert settings.GOBLIN_BEAM_COLOR == (0,200,0); assert settings.SUPERMAN_BEAM_COLOR == (200,0,0); print('settings.py OK')"</automated>
  </verify>
  <done>settings.py exists at repo root. All 14 constants are importable. The python -c assertion command above exits 0 and prints "settings.py OK".</done>
</task>

<task type="auto">
  <name>Task 2: Project scaffold — dependency files, .gitignore, pytest.ini</name>
  <files>requirements.txt, requirements-dev.txt, .gitignore, pytest.ini</files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md (Standard Stack section — exact pinned versions; requirements.txt structure)
    .planning/phases/01-foundation/01-VALIDATION.md (Test Framework section — pytest.ini config required fields)
  </read_first>
  <action>
    Create requirements.txt with a single runtime dependency:
      pygame-ce==2.5.7
    (No other runtime dependency — Pillow, pyfluidsynth, pytest are dev-only.)

    Create requirements-dev.txt that extends requirements.txt:
      -r requirements.txt
      Pillow==12.2.0
      pyfluidsynth==1.3.4
      pytest==9.0.3
      pyinstaller==6.20.0
    (pyinstaller is Phase 6 only but listed here for future pip install -r convenience.)

    Create .gitignore. Must exclude:
      assets/          (generated by convert_assets.py — never committed)
      __pycache__/
      *.pyc
      *.pyo
      .venv/
      venv/
      *.egg-info/
      dist/            (PyInstaller output)
      build/           (PyInstaller output)
      .pytest_cache/
      *.spec           (PyInstaller spec files)
    Do NOT gitignore raw_assets/ — it is committed source. Do NOT gitignore tools/ — soundfont is committed.

    Create pytest.ini with:
      [pytest]
      testpaths = tests
      python_files = test_*.py
      python_classes = Test*
      python_functions = test_*
      addopts = -x -q
    (The -x flag stops on first failure; -q reduces noise. Both are overridable on the command line.)
  </action>
  <verify>
    <automated>python -c "import pathlib; r=pathlib.Path('requirements.txt').read_text(); assert 'pygame-ce==2.5.7' in r; rd=pathlib.Path('requirements-dev.txt').read_text(); assert 'Pillow==12.2.0' in rd; assert 'pytest==9.0.3' in rd; gi=pathlib.Path('.gitignore').read_text(); assert 'assets/' in gi; assert 'raw_assets' not in gi; pi=pathlib.Path('pytest.ini').read_text(); assert 'testpaths = tests' in pi; print('Scaffold OK')"</automated>
  </verify>
  <done>requirements.txt, requirements-dev.txt, .gitignore, and pytest.ini all exist with correct content. The python -c assertion exits 0 and prints "Scaffold OK".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings.py → all modules | All game modules import constants from settings.py; wrong values here cascade into every future phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | settings.py constants | accept | Local dev file only; no user input; values are visible in source |
| T-01-02 | Tampering | requirements.txt pin | mitigate | Exact version pins (==) prevent silent upgrades to malicious/broken versions |
| T-01-SC | Tampering | pip install from requirements files | mitigate | All packages pre-audited via slopcheck [OK]; no [ASSUMED]/[SUS] packages in this plan |
</threat_model>

<verification>
After both tasks complete:
  python -c "import sys; sys.path.insert(0, '.'); import settings; print(settings.SCREEN_W, settings.SKY_COLOR)"
  Expected output: 1280 (0, 255, 255)

  cat requirements.txt | grep pygame-ce
  Expected: pygame-ce==2.5.7

  cat .gitignore | grep "^assets/"
  Expected: assets/

  cat pytest.ini | grep testpaths
  Expected: testpaths = tests
</verification>

<success_criteria>
- settings.py imports cleanly and all 14 constants match the exact values specified in RESEARCH.md Pattern 4
- .gitignore excludes assets/ but does NOT exclude raw_assets/ or tools/
- requirements.txt has exactly pygame-ce==2.5.7 as the only runtime dependency
- requirements-dev.txt includes -r requirements.txt plus Pillow, pyfluidsynth, pytest, pyinstaller at pinned versions
- pytest.ini configures testpaths = tests with -x -q as default addopts
</success_criteria>

<output>
Create .planning/phases/01-foundation/01-01-SUMMARY.md when done
</output>
