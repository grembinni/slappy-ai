---
phase: 03-combat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - settings.py
autonomous: true
requirements:
  - CMB-04
  - CMB-06

must_haves:
  truths:
    - "settings.BEAM_LENGTH == 40"
    - "settings.BEAM_WIDTH == 3"
  artifacts:
    - path: "settings.py"
      provides: "Phase 3 beam geometry constants"
      contains: "BEAM_LENGTH = 40"
  key_links:
    - from: "settings.py"
      to: "beam.py"
      via: "from settings import BEAM_LENGTH, BEAM_WIDTH, BEAM_SPEED, SCREEN_W, DIR_LEFT, DIR_RIGHT"
      pattern: "from settings import"
---

<objective>
Append two Phase 3 beam geometry constants to settings.py under a new comment block. These are the single source of truth for beam visual dimensions — beam.py imports them and never defines its own numeric literals.

Purpose: BEAM_LENGTH and BEAM_WIDTH must exist in settings.py before beam.py is written (CLAUDE.md: all geometry constants in settings.py, never hardcode pixel values).
Output: settings.py extended with BEAM_LENGTH = 40 and BEAM_WIDTH = 3
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/03-combat/03-CONTEXT.md
@settings.py
</context>

<interfaces>
<!-- Existing settings.py constants (do NOT change these): -->
<!-- SCREEN_W = 1280, SCREEN_H = 800, FPS = 60, SPRITE_SIZE = 128 -->
<!-- BEAM_SPEED = 400, GOBLIN_BEAM_COLOR, SUPERMAN_BEAM_COLOR -->
<!-- DIR_UP=1, DIR_DOWN=2, DIR_LEFT=3, DIR_RIGHT=4, DIR_POSE=5, DIR_IDLE=6 -->

<!-- New constants to append (exact names and values — per D-01, D-07): -->
<!-- BEAM_LENGTH = 40   # px — visual bolt length and wrap threshold -->
<!-- BEAM_WIDTH = 3     # px — pygame.draw.line width argument -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Append Phase 3 beam constants to settings.py</name>
  <files>settings.py</files>
  <read_first>
    settings.py (read the full file to find the correct append point)
    .planning/phases/03-combat/03-CONTEXT.md (D-01: 40px bolt, 3px thick; D-07: BEAM_LENGTH = 40)
    CLAUDE.md ("All geometry constants in settings.py — never hardcode pixel values")
  </read_first>
  <action>
    Open settings.py and append the following block at the very end of the file. Do NOT modify any existing constants. Do NOT add any executable code.

    Append exactly this block:

      # Beam (Phase 3)
      BEAM_LENGTH = 40   # px — visual bolt length and wrap threshold (D-01, D-07)
      BEAM_WIDTH = 3     # px — pygame.draw.line width argument (D-01)

    After appending, the file must still be importable with no errors and all existing Phase 1/2 constants must remain unchanged.
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, '.'); import settings; assert settings.BEAM_LENGTH == 40; assert settings.BEAM_WIDTH == 3; assert settings.BEAM_SPEED == 400; assert settings.SCREEN_W == 1280; print('settings phase3 OK')"</automated>
  </verify>
  <done>settings.py has both new constants at the correct values. All existing constants are unchanged. The python -c assertion above exits 0 and prints "settings phase3 OK".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings.py → all modules | Beam constants flow into beam.py; wrong values affect gameplay feel but not security |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | settings.py constants | accept | Local dev file only; no user input; values are visible in source and validated by automated verify command |
| T-03-SC | Tampering | pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After task completes:

  python -c "import settings; print(settings.BEAM_LENGTH, settings.BEAM_WIDTH)"
  Expected output: 40 3

  python -c "import settings; print(settings.BEAM_SPEED, settings.SCREEN_W, settings.DIR_LEFT)"
  Expected output: 400 1280 3
  (Confirms existing constants are untouched)
</verification>

<success_criteria>
- settings.BEAM_LENGTH == 40
- settings.BEAM_WIDTH == 3
- All Phase 1/2 constants (SCREEN_W, SCREEN_H, FPS, SPRITE_SIZE, BEAM_SPEED, GOBLIN_BEAM_COLOR, SUPERMAN_BEAM_COLOR, WIN_SCORE, DIR_*, PLAYER_SPEED_*, ANIM_INTERVAL, DISPLAY_SPRITE_SIZE) are unchanged
- settings.py remains free of executable code — constants only
</success_criteria>

<output>
Create .planning/phases/03-combat/03-01-SUMMARY.md when done
</output>
